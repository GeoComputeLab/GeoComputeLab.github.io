# site properties and page defaults
title: GeoCompute Lab 
subtitle: 
description: The GeoCompute Lab, based in the Geography Department at Virginia Tech, advances research at the intersection of geospatial computing, geospatial data science and AI. The lab develops scalable computational solution and data-driven insights to address complex urban, environmental, and societal challenges.
header: images/background.jpg
footer: images/background.jpg
proofer: false

# site social media and other links
links:
  email: fangzheng@vt.edu
  orcid: 0000-0001-5180-0380
  google-scholar: https://scholar.google.com/citations?user=FNN4IasAAAAJ&hl=en
  github: GeoComputeLab

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
